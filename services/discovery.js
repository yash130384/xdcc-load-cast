// Discovery service deactivated in favor of client-side VLC streaming.
// No SSDP, mDNS, or Bonjour polling is performed, saving CPU and network resources.

export function startAllDiscovery(appState) {
  // Discovery disabled
}

export function updateAllDiscovery(appState) {
  // Discovery disabled
}